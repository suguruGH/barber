<div class="container">
  <div class="row">
    <div class="col-xs-12">
<%= form_with(model: @share, local: true) do |form| %>
<% if @share.errors.any? %>
    <div id="error_explanation">
      <h2><%= @share.errors.count %>件のエラーがあります。</h2>
      <ul>

      <% @share.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
<% end %>
  <div class="share_title">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div class="share_title">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image, id: :share_image %>
    <%= form.hidden_field :image_cache %>
  </div>-->
  <%= form.submit %>
<% end %>
    </div>
  </div>
</div>
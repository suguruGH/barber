<%= form_with(model: stylist, local: true) do |form| %>
  <% if stylist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stylist.errors.count, "error") %> prohibited this stylist from being saved:</h2>

      <ul>
      <% stylist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :stylist_name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age, id: :stylist_age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
